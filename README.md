# DmrPoolSystem
A robust, zero-allocation object pooling system for Unity. Created for stability with recursion-safe nesting, O(1) double-return protection, and auto-expanding buffers. Eliminates Garbage Collection spikes in complex, high frequency simulations.
